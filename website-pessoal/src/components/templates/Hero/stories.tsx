import { Meta, StoryObj } from "@storybook/react";
import Hero from ".";

export default{
    title: 'Components/Templates/Hero',
    component: Hero
}as Meta<StoryObj>

type Story = StoryObj;

export const Template:Story = {}