import { sections } from "@/constants/sections"
import './style.css'

const Hero = () => {
  return (
    <section id={sections.hero} className="footer">
        <h1>Hero</h1>
    </section>
  )
}

export default Hero