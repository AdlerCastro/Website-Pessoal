import { sections } from "@/constants/sections"

//Styles
import './style.css'

const Hero = () => {

  return (
    <section id={sections.hero} className="hero">

    </section>
  )
}

export default Hero