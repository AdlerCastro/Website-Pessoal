import { Meta, StoryObj } from '@storybook/react';
import About from '.';

export default {
  title: 'Components/Templates/About',
  component: About,
} as Meta<StoryObj>;

type Story = StoryObj;

export const Default: Story = {};
